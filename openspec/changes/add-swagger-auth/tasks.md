# Задачи имплементации интеграции Swagger Auth

## 1. Конфигурация окружения

- [x] 1.1 Добавить переменную окружения `ENABLE_SWAGGER_UI` в `app/core/config.py` с значением по умолчанию `True`
- [x] 1.2 Добавить `ENABLE_SWAGGER_UI` в файл `.env.example` с комментарием о назначении
- [x] 1.3 Обновить `.env.test` установить `ENABLE_SWAGGER_UI=true` для тестирования

## 2. Интеграция HTTPBearer в FastAPI

- [x] 2.1 Импортировать `HTTPBearer` из `fastapi.security` в `app/main.py`
- [x] 2.2 Создать экземпляр `HTTPBearer(auto_error=False)` для использования в документации
- [x] 2.3 Добавить параметр конфигурации OpenAPI в конструктор FastAPI с наименованием security scheme "Bearer"
- [x] 2.4 Применить условную логику: если `ENABLE_SWAGGER_UI=false`, то `docs_url=None` и `redoc_url=None`

## 3. Применение security scheme к эндпоинтам

- [x] 3.1 Добавить `security=[HTTPBearer()]` параметр к всем защищённым роутам в `app/api/v1/`
- [x] 3.2 Проверить эндпоинты: `/api/v1/sessions`, `/api/v1/password-reset/confirm` и другие защищённые endpoints
- [x] 3.3 Убедиться, что обновление `get_current_user` в `app/core/dependencies.py` не требуется

## 4. Тестирование Swagger UI в локальной среде

- [x] 4.1 Запустить приложение локально и открыть `http://localhost:8003/docs`
- [x] 4.2 Проверить, что кнопка "Authorize" видна в верхнем правом углу
- [x] 4.3 Нажать "Authorize" и проверить, что поле для ввода Bearer token появляется
- [x] 4.4 Получить AccessToken через эндпоинт `/oauth/token` (используя тестовые учётные данные)
- [x] 4.5 Ввести полученный токен в диалоговое окно авторизации

## 5. Функциональное тестирование защищённых эндпоинтов

- [x] 5.1 В Swagger UI проверить, что защищённые эндпоинты помечены иконкой замка
- [x] 5.2 Выполнить запрос к защищённому эндпоинту (например, GET `/api/v1/sessions`) с авторизованным токеном
- [x] 5.3 Убедиться, что запрос выполняется успешно (200 OK)
- [x] 5.4 Выполнить запрос БЕЗ токена или с невалидным токеном и проверить, что возвращается 401 Unauthorized
- [x] 5.5 Проверить, что заголовок `Authorization: Bearer <token>` автоматически добавляется Swagger UI

## 6. Интеграционное тестирование

- [x] 6.1 Запустить существующие интеграционные тесты `pytest tests/integration/` для проверки, что ничего не сломалось
- [x] 6.2 Проверить что авторизация через `get_current_user` продолжает работать как раньше
- [x] 6.3 Проверить что статус-коды ошибок авторизации остаются неизменными

## 7. Тестирование с отключённой Swagger

- [x] 7.1 Установить `ENABLE_SWAGGER_UI=false` в переменных окружения
- [x] 7.2 Перезагрузить приложение
- [x] 7.3 Проверить, что `/docs` возвращает 404 или перенаправляет на главную страницу
- [x] 7.4 Убедиться, что API endpoints всё ещё работают (отключена только документация)

## 8. Документация и финализация

- [x] 8.1 Обновить README.md с информацией о переменной окружения `ENABLE_SWAGGER_UI`
- [x] 8.2 Добавить пример использования Swagger UI с Bearer token авторизацией в TECHNICAL_SPECIFICATION.md
- [x] 8.3 Проверить что все изменения соответствуют requirements из spec файлов
- [x] 8.4 Создать Git commit с сообщением "Add OAuth2 Bearer authentication to Swagger UI"
